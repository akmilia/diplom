<Window x:Class="diplom.Components.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:diplom.Components"
        Title="Сообщение" Height="200" Width="400" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" WindowStyle="ToolWindow" ShowInTaskbar="False">
    <!--<Border CornerRadius="10" BorderBrush="#2196F3" BorderThickness="2">
        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Source="/Images/logo.png" Width="48" Height="48" Grid.Column="0"/>
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="18" Foreground="#2196F3"/>
                <TextBlock Text="{Binding Message}" FontSize="15" Margin="0,10,0,20"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="ОК" Width="100" Margin="5" Click="Ok_Click"/>
                    <Button Content="Отмена" Width="100" Margin="5" Click="Cancel_Click" Visibility="{Binding ShowCancel, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>-->
</Window>